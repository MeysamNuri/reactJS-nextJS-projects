import {permanentRedirect,redirect} from 'next/navigation'

const Series = () => {
    // permanentRedirect("/about")
    // redirect("/about") temporary redirect
    return ( 
        <h1>
            Series page
        </h1>
     );
}
 
export default Series;